# Launch post (DRAFT, hold)

**Do not post yet.** Hold until: (1) the site is live and final, and (2) a real before/after
screenshot is ready (agent cold vs agent + the rules). The first comment must be that screenshot.
It's the proof and the most shareable asset. Don't post without it.

The credit line (Pocock / others) stays. Don't claim novelty.

---

Your AI writes code like a junior who's read every book and shipped nothing.

Confident, tidy, completely over-engineered. Ask it for a simple app and it'll happily give you a database anyone can read, a file upload that leaks, a server that hands out its own cloud keys. It won't warn you. It doesn't know to.

So I wrote down the bits it gets wrong.

It's a page you point your agent at. "Read chrisnorthfield.com/rules/dictionary.md, then build it." It reads it and builds the safe version instead of the naive one. The migration that won't lock your table. The upload stored somewhere it won't vanish. The questions a senior would ask before writing a line.

Same prompt, two builds. The difference isn't the model. One of them was told how someone who's shipped before would do it.

It works whether you're an engineer or someone who learned what an API was last week. Free, no signup, nothing to buy.

It builds on the stuff Matt Pocock and others started (getting the agent to slow down and think first). This is the secure-web-app version, the things fifteen years teaches you and no model has lived.

chrisnorthfield.com/rules

AI will build you a working app. It won't build you a safe one. Now it will.
